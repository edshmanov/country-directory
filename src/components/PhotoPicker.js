import { useRef } from "react";
export default function PhotoPicker({ preview, setPreview, label, subLabel }) {
  const ref = useRef(null);
  const uid = "ph-" + Math.random().toString(36).slice(2, 6);
  const handleFile = (e) => { const f = e.target.files?.[0]; if (!f) return; const r = new FileReader(); r.onload = (ev) => setPreview(ev.target.result); r.readAsDataURL(f); };
  return (<div style={{ marginBottom: 4 }}><p style={{ fontSize: 14, fontWeight: 700, color: "#3d2c1e", margin: "0 0 8px" }}>📷 {label}</p><input type="file" accept="image/*" id={uid} ref={ref} onChange={handleFile} style={{ position: "absolute", width: 1, height: 1, opacity: 0 }} />{preview ? (<div style={{ position: "relative", borderRadius: 12, overflow: "hidden", border: "3px solid var(--gn)" }}><img src={preview} alt="" style={{ width: "100%", height: 200, objectFit: "cover", display: "block" }} /><div style={{ position: "absolute", bottom: 0, left: 0, right: 0, background: "rgba(74,124,89,0.92)", color: "#fff", padding: 8, fontSize: 14, fontWeight: 700, textAlign: "center" }}>✅ {subLabel}</div><button onClick={(e) => { e.preventDefault(); setPreview(null); }} style={{ position: "absolute", top: 8, right: 8, background: "rgba(0,0,0,0.6)", color: "#fff", border: "none", borderRadius: "50%", width: 32, height: 32, cursor: "pointer", fontSize: 16, fontWeight: 700, padding: 0 }}>✕</button></div>) : (<label htmlFor={uid} style={{ display: "flex", flexDirection: "column", alignItems: "center", width: "100%", padding: "28px 14px", background: "#f0ebe3", border: "3px dashed #8b7355", borderRadius: 12, cursor: "pointer", textAlign: "center", gap: 6 }}><span style={{ fontSize: 44 }}>📷</span><span style={{ fontSize: 15, color: "#5c4a3a", fontWeight: 700 }}>Tap to add photo</span><span style={{ fontSize: 12, color: "#8b7355" }}>JPG, PNG, HEIC</span></label>)}</div>);
}
